# Contributing to Script Memory

## The One Rule

**Never commit an unverified solution.**

A script with `status: draft` in metadata.yaml must not be committed
to the main branch. Test it first. Then commit it as `status: verified`.

## How to Add a New Entry

### 1. Create the folder
